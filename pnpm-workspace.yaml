# pnpm 11 requires an explicit build-script decision per dependency (strictDepBuilds).
# These are all trusted native-binary packages in the Nuxt/Vite toolchain.
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  maplibre-gl: true
  unrs-resolver: true
  vue-demi: true
